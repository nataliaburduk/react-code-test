import styled from 'styled-components';

export const LocationsCards = styled.div`
  padding: 25px 30px;
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(auto, 340px));
  justify-content: center;
  gap: 25px;
`;
